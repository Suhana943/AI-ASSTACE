name: Auto Intel Laptop Scraper

on:
  workflow_dispatch: 
  schedule:
    - cron: '0 0 * * *' 

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write 
    steps:
      - name: Checkout code
        uses: actions/checkout@v4  # v3 se v4 kar diya

      - name: Setup Node.js
        uses: actions/setup-node@v4  # v3 se v4 kar diya
        with:
          node-version: 20

      - name: Install dependencies
        run: |
          npm cache clean --force
          npm install axios cheerio

      - name: Run Scraper Script
        run: node scraper.js

      - name: Commit and Push laptops.json
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "actions@github.com"
          
          # Check karein agar file bani hai toh hi push karein
          if [ -f "laptops.json" ]; then
            git add laptops.json
            git commit -m "Auto-update: Laptops data" || exit 0
            git push
          else
            echo "Error: laptops.json file generate nahi hui!"
            exit 1
          fi
          
